GIT

.gitignore - lägg till filer
    - *.class
    - run
    - build
git add "filename" - lägg till fil till commit
git commit -m "write message here" - lägg till commit och skriv meddelande 
git push - pushar comit (till master?)
git push --set-upstream orgin "branchname"

git branch -a
git checkout "name - byt branch
git branch "NAME" -> skapa branch
git checkout -b "NAME" -> skapa branch och byt till den

Several branches update
    - synch with master
git checkout master
git pull
git checkout "teobranch" 
git merge master

    - push to master
git checkout master
git merge "teobranch"
git push orgin master